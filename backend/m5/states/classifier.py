"""Task 4 (brief §4) — `classify_primary(row) -> PrimaryState` and
`apply_modifiers(state, modifiers) -> StateKey`, pure functions, no I/O,
tested against hand-worked reference values. Blocked on `definitions.py`
(Appendix A's real thresholds — see that module's own docstring for
exactly what's missing). Not yet implemented."""
