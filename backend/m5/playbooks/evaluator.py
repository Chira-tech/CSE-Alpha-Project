"""Task 7 (brief §7) — runs a registered playbook's entry/exit
conditions against the panel and produces its ten-gate report card
(`m5.validation.gates`). Every playbook's entry MUST include
`ExistingGatesPass()` and `NetExpectedReturnAboveFloor()` — a test
asserting this for every definition is part of this task's own
acceptance bar, not optional. Not yet implemented."""
