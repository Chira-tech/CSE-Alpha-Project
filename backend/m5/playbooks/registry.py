"""Task 7 (brief §7) — registers all six playbook definitions into
`m5.trials` (their mechanism, closing_mechanism, and prior) BEFORE any
of them is ever backtested. Depends on `m5.playbooks.definitions.*` and
`m5.validation.trials`. Not yet implemented."""
