"""Task 5.1 (brief §5.1) — `block_bootstrap(panel, state_key, horizon,
n_reps=2000, block_months=12)`: contiguous-block resampling with
replacement, preserving cross-sectional correlation within each sampled
block. `block_months` must always equal the horizon under test (§0.5
D1(b): 6-month is now the PRIMARY validation horizon, per the 21 Aug
2026 decision — 12-month stays secondary). Not yet implemented."""
