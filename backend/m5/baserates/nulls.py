"""Task 5.3 (brief §5.3) — the matched null: random draws from the
investable universe over identical calendar windows, matched on sector
and size decile, 2,000 reps. Every `BaseRateResponse` must carry one
(a REQUIRED Pydantic field, never `Optional`) — enforced at the schema
level once `m5.api.schemas` defines that model (Task 5). Not yet
implemented."""
