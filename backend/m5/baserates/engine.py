"""Task 5 (brief §5) — the base rate statistics (hit rate, median excess
return, CI, effective N vs naive N) for every state and permitted
state+modifier combination, gated at n_effective < 8 ->
`InsufficientEvidence`. Depends on `m5.panel.builder` (Task 2) and
`m5.states.classifier` (Task 4). Not yet implemented."""
