"""Task 6.4 (brief §6.4) — spec §7.4's ten gates, each an independent
boolean function returning `(passed, actual_value, threshold,
explanation)`. Also where Task 8b's holdout replay (§0.5 D3) and the
double CGT run (0% and 15%, §0.5 D2) get evaluated — a playbook that
degrades more than 40% in holdout replay does not advance to shadow.
Not yet implemented."""
