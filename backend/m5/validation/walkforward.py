"""Task 6.2 (brief §6.2) — `purged_walkforward(panel, horizon_months,
n_folds=5, embargo_months=1)`: purge length always equals the horizon
under test, no parameter to shorten it (brief §11's own explicit "do
not" list). Not yet implemented."""
