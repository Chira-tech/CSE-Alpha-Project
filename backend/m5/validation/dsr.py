"""Task 6.3 (brief §6.3) — the Deflated Sharpe Ratio, reading the LIVE
trial count from `m5.trials` where `counted_in_dsr` — no parameter
anywhere to exclude a trial (brief §11's own explicit "do not" list).
Not yet implemented."""
