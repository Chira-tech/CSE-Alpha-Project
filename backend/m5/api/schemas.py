"""Task 5+ builds the real response models here (BaseRateResponse with
its mandatory `null` field, playbook report cards, trial records, ...).
Empty at the Task 1 (isolation scaffold) stage — nothing to schema yet."""
